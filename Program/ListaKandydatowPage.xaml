<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="PIVRekrutacjaUczelnia.ListaKandydatowPage"
             Title="Lista Kandydatów">
    <CollectionView x:Name="kandydaciView">
        <CollectionView.ItemTemplate>
            <DataTemplate>
                <VerticalStackLayout Padding="10">
                    <Label Text="{Binding Imie}" FontAttributes="Bold" FontSize="18" />
                    <Label Text="{Binding Nazwisko}" FontAttributes="Bold" FontSize="18" />
                    <Label Text="{Binding Kierunek}" FontSize="16" />
                    <Label Text="{Binding Rodzaj}" FontSize="16" />
                    <Label Text="{Binding Suma_punktow}" FontSize="16" />
                    <Label Text="{Binding Status_przyjecia}" FontSize="16" />
                </VerticalStackLayout>
            </DataTemplate>
        </CollectionView.ItemTemplate>
    </CollectionView>
</ContentPage>